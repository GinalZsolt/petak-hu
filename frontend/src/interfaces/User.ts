import type { Ban } from './Ban';

export interface User {
    ID:number,
    email:string,
    name:string,
    fullname:string,
    passwd:string,
    imagefile:string,
    phone:string,
    address:string,
}